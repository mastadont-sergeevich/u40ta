/**
 * DTO для ответа после успешной авторизации
 * В новой системе всегда возвращает access_token
 * Статус авторизации не нужен - все авторизованные получают доступ
 */
export class AuthResponseDto {
  access_token: string;
}